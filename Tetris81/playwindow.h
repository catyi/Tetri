#ifndef PLAYWINDOW_H
#define PLAYWINDOW_H

#include <QMainWindow>

class PlayWindow : public QMainWindow
{
    Q_OBJECT
public:
    explicit PlayWindow(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // PLAYWINDOW_H